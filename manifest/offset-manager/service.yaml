apiVersion: v1
kind: Service
metadata:
  name: offset-manager
  namespace: app
  labels:
    app: offset-manager
spec:
  type: ClusterIP
  selector:
    app: offset-manager
  ports:
    - port: 8080
      targetPort: 8080
